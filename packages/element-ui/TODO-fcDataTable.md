# fc 组件能力闭环 TODO

## 已完成

- [x] 将全部包内 fc 运行时组件收口到统一组件清单。
- [x] 将运行时清单与设计器适配清单分层，避免生效页打入设计器代码。
- [x] 提供通用值读取、props 解析、规则树绑定和 form-create API 更新能力。
- [x] 提供统一真实组件预览，并由 capability 声明表格单元格预览支持。
- [x] 公开 form-create 原生 JSON codec，支持函数配置往返。
- [x] 保留 `chartData`、`data` 历史配置，同时支持统一 `modelValue`。
- [x] `fcDataTable` 完成同步/异步回调、确认、独立 loading、去重提交、成功/失败/最终事件。
- [x] 行操作设计器完成草稿、取消、确定、必填/唯一校验，并保留未知字段。
- [x] 更新中英文 locale、TypeScript 公共类型、独立示例和运行时文档。

## 发布前

- [x] 在浏览器中执行独立示例的确认、取消、异步成功/失败、草稿提交/取消和必填校验交互回归。
- [ ] 对当前完整改动执行主包与运行时包构建验证（本轮按要求未执行构建）。
- [ ] 由发布负责人确定版本号并发布预发布包。
- [ ] 消费项目升级产物后，删除宿主中已经由组件清单、值绑定和预览能力替代的类型分支。

## 不属于组件库

- PMT 页面 schema、页面权限、业务接口适配和 LowCode 页面编排。
- 将组件行为桥接到 PMT `CustomAction` 或向 form-create API 注入 LowCode 专用对象。
- 任务、文件等具体业务能力。
